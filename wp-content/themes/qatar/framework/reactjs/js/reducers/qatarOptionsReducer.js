const initialState = {
    qatarOptions: wp_qatar_reactjs,
};

const qatarOptionsReducer = (state = initialState, action) => {
    const { type, payload } = action;

    switch (type) {
        default:
            return state;
    }
};

export default qatarOptionsReducer;
